[package]
name = "python"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
name = "stacrs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = "0.22"
stac = { version = "0.8", path = "../stac", features = ["reqwest"] }
stac-validate = { version = "0.2", path = "../stac-validate" }
